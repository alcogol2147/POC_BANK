import { ColumnFirstDirectiveDirective } from './column-first-directive.directive';

describe('ColumnFirstDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnFirstDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
