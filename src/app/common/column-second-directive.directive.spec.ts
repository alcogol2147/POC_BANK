import { ColumnSecondDirectiveDirective } from './column-second-directive.directive';

describe('ColumnSecondDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnSecondDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
